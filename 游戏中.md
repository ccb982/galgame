- SceneController.getScenesByGameId()

- 请求 ： GET /api/games/{gameId}/scenes
- 用途 ：游戏启动时加载所有场景（一次性获取）
- 返回 ：游戏的所有场景列表
- 使用时机 ：游戏初始化阶段
- SceneController.getScene()

- 请求 ： GET /api/games/{gameId}/scenes/{sceneKey}
- 用途 ：获取特定场景的详细信息
- 返回 ：单个场景对象
- 使用时机 ：场景切换时（如果不使用缓存）




